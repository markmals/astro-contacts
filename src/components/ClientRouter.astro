---
import { ClientRouter as ViewTransitionsRouter } from "astro:transitions";
import { unsafe_provideServerParams, unsafe_provideServerURL } from "../lib/router.ts";

unsafe_provideServerURL(Astro.url);
unsafe_provideServerParams(Astro.params);
---

<ViewTransitionsRouter fallback="none" />
